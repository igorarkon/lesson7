ѕриведенный выше пример небольшой программы показывает создание небольшого массива lines, 
который содержит три строковых элемента: УfirstФ, УsecondФ и УthirdФ. 
«а счет функции open и св€зки операторов with as происходит открытие текстового файла test.txt
в корневом каталоге жесткого диска D.
¬ цикле мы проходим по всем элементам списка и с помощью команды write их записываем. 
„тобы кажда€ запись была с новой строки, добавл€ем символ перевода строки.